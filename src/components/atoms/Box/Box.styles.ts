import styled from '@emotion/styled';
import { border, color, flexbox, grid, layout, shadow, space, typography } from 'styled-system';


export const BoxStyled = styled.div({
  boxSizing: "border-box",
}, space, color, flexbox, layout, typography, border, grid, shadow);
